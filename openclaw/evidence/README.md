# Phase 2 acceptance evidence

`phase2-acceptance.json` is generated by `make verify` after all authenticated
Open WebUI end-to-end scenarios pass. It records response durations, SHA-256
hashes of final answers, contract sources, and sampled CPU/memory maxima.

The record intentionally excludes credentials, gateway tokens, user passwords,
raw Kubernetes logs, complete prompts, and response bodies.
