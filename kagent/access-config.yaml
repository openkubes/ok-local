kind: KagentAccessProfile
mode: read-only

install:
  namespace: kagent

read:
  scope: cluster
  secrets: false
  tools:
    - k8s_get_resources
    - k8s_describe_resource
    - k8s_get_events
    - k8s_get_pod_logs
    - k8s_get_resource_yaml
