# OK-137 — ok-local evidence

Date: 2026-08-05  
Cluster: `ok-infra-local` (K3s v1.35.5+k3s1)  
Registry: zot v2.1.20, chart 0.1.122  
Conformance image: `ghcr.io/opencontainers/distribution-spec/conformance@sha256:ad756107873b1478603b6ed9024d868eb1b2adeba5bf3c0d2bceebad517f7f36`  
Storage: 8 GiB `local-path` PVC  
Scope: Phase 1 / envelope-invariant mechanics only

## Scope results

| Requirement | Result | Evidence |
|---|---|---|
| curated zot composition on K3s | PASS | StatefulSet `zot` Ready 1/1, Service and PVC Bound |
| OCI image push and pull | PASS | `smoke.sh` step 1 |
| OCI Helm chart push and pull | PASS | `smoke.sh` step 3 |
| pull by immutable digest | PASS | Alpine index digest preserved |
| OCI Referrers | PASS | SPDX JSON SBOM attached and discovered |
| separated credentials | PASS | `admin`, `ci`, read-only `puller`; pull succeeds and push is denied |
| metrics | PASS | authenticated `/metrics` returned Prometheus data |
| garbage collection | PASS | disposable orphan reclaimed; referenced control blob preserved |
| storage integrity | PASS | startup scrub completed for retained lifecycle repository without `affected` content |
| offline-transfer mechanics | PASS | OCI layout export, SHA-256 verify, import, pull-by-digest |
| GitOps wiring | PASS | pinned Kustomize/Helm render plus Argo CD Application |
| OCI Distribution conformance | PASS | 848 pass, 0 fail, 0 error, 4 skip, 2 disabled |

## Local evidence paths

- Smoke: `contract/tests/results-smoke/20260805t095709z/`
- Offline transfer: `contract/tests/results-offline/20260805t095745z/`
- GC and scrub: `contract/tests/results-lifecycle/20260805t095755z/`
- OCI conformance: `contract/tests/results/20260805t095749z/`
  - `results.yaml`
  - `junit.xml`
  - `report.html`

These four directories come from one successful sequential review-remediation
run of `make smoke`, `make offline-transfer`, `make conformance`, and
`make lifecycle`, matching the order of the documented `make contract` target.
The README deployment sequence (`make repo`, `make secret`, `make deploy`,
`make status`) and the final `make verify` command also completed successfully.

The review-remediation run used a newly created, temporary `DOCKER_CONFIG` for
the smoke, offline-transfer, and lifecycle clients. The smoke test used a unique
denied-write repository for this run and asserted the Referrers JSON subject
digest plus an `application/spdx+json` descriptor before reporting PASS.

Generated reports are intentionally git-ignored and should be attached to the
OK-137 ticket. This Markdown file is the durable, reviewable summary.

## Promotion boundary

This demonstrates mechanics and envelope-invariant behavior on local K3s. It
does not replace Phase-2 validation on Talos/ok-shared. OIDC, ingress/TLS,
production storage, backup/restore, upgrade/rollback, release-scale transfer,
and DR remain subject to OK-138 and the corresponding platform ADRs.
